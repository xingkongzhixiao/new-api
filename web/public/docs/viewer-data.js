/* 由 build-viewer-data.cjs 生成，请勿手改 */
window.VIEWER_DOCS = [{"id":"nodejs","title":"Node.js 环境安装","md":"# Node.js 环境安装教程\n\n> 在 Windows、macOS、Linux 安装 Node.js LTS，并验证 `node` / `npm` 命令可用。\n\n后续的 Claude Code、Gemini CLI、Codex 都依赖 Node.js 运行环境。**请先完成本教程**，再继续各自工具配置。\n\n## 版本要求\n\n| 工具 | Node.js 版本 |\n|------|----------------|\n| Claude Code、Gemini CLI | ≥ 18 |\n| Codex CLI | ≥ 22（推荐） |\n\n---\n\n## Windows\n\n### 方法一：官方下载（推荐）\n\n前往 [Node.js 官网](https://nodejs.org) 下载 **LTS** 版本，双击安装包按提示安装。安装时勾选 **Add to PATH**，确保命令行可用。\n\n### 方法二：Chocolatey\n\n```powershell\nchoco install nodejs-lts\n```\n\n### 方法三：Scoop\n\n```powershell\nscoop install nodejs-lts\n```\n\n### 方法四：nvm-windows（多版本管理）\n\n1. 前往 [nvm-windows Releases](https://github.com/coreybutler/nvm-windows/releases) 下载 `nvm-setup.exe` 并安装。\n2. 在终端执行：\n\n```powershell\nnvm install lts\nnvm use lts\n```\n\n---\n\n## macOS\n\n### 方法一：Homebrew（推荐）\n\n```bash\nbrew install node\n```\n\n### 方法二：官方安装包\n\n前往 [Node.js 官网](https://nodejs.org) 下载 macOS 安装包。\n\n### 方法三：nvm（多版本管理，适合开发者）\n\n```bash\n# 安装 nvm\ncurl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.0/install.sh | bash\n\n# 重新加载配置\nsource ~/.zshrc   # 或 source ~/.bashrc\n\n# 安装并使用 LTS\nnvm install --lts\nnvm use --lts\n```\n\n---\n\n## Linux\n\n### Ubuntu / Debian\n\n```bash\ncurl -fsSL https://deb.nodesource.com/setup_lts.x | sudo -E bash -\nsudo apt-get install -y nodejs\n```\n\n### CentOS / RHEL / Fedora\n\n```bash\ncurl -fsSL https://rpm.nodesource.com/setup_lts.x | sudo bash -\nsudo yum install -y nodejs\n# Fedora 也可使用：sudo dnf install -y nodejs\n```\n\n### Arch Linux\n\n```bash\nsudo pacman -S nodejs npm\n```\n\n### nvm（各发行版通用）\n\n```bash\ncurl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.0/install.sh | bash\nsource ~/.bashrc\nnvm install --lts\nnvm use --lts\n```\n\n---\n\n## 验证安装\n\n安装完成后在终端执行：\n\n```bash\nnode --version\nnpm --version\n```\n\n若能输出版本号，说明 Node.js 与 npm 已可用。\n\n> **提示（Windows）**：若安装后命令仍不可用，请**重新打开终端**，或检查是否已将 Node.js 加入系统 `PATH`。\n\n---\n\n## 下一步\n\n- [Claude Code 配置教程](?doc=claude)\n- [Gemini CLI 配置教程](?doc=gemini)\n- [Codex 配置教程](?doc=codex)\n"},{"id":"claude","title":"Claude Code 配置","md":"# Claude Code 配置教程\n\n> 安装并配置 Claude Code CLI，连接 Ai Go Code 平台。\n\n[Claude Code](https://docs.anthropic.com/en/docs/claude-code) 是 Anthropic 官方 CLI，可作为 AI 编程助手使用。本教程覆盖安装与连接配置。\n\n**前置条件：** 请先完成 [Node.js 环境安装](?doc=nodejs)（需要 **Node.js ≥ 18**）。\n\n---\n\n## 第一步：安装 Claude Code\n\n在终端（Windows 可用 PowerShell 或 CMD）执行：\n\n```bash\nnpm install -g @anthropic-ai/claude-code\n```\n\n若在 macOS / Linux 上遇到权限问题，可使用 `sudo`：\n\n```bash\nsudo npm install -g @anthropic-ai/claude-code\n```\n\n### 验证安装\n\n```bash\nclaude --version\n```\n\n若显示版本号，说明安装成功。\n\n---\n\n## 第二步：设置环境变量\n\n为让 Claude Code 连接到 Ai Go Code，需设置：\n\n| 变量 | 说明 |\n|------|------|\n| `ANTHROPIC_BASE_URL` | 服务地址 |\n| `ANTHROPIC_AUTH_TOKEN` | 你的 API Key |\n\n> **说明：** Ai Go Code 使用 `ANTHROPIC_AUTH_TOKEN` 认证。若直连 Anthropic 官方 API，则使用 `ANTHROPIC_API_KEY`。**两者不要同时设置。**\n\n### 临时设置（当前会话）\n\n**Windows（PowerShell）**\n\n```powershell\n$env:ANTHROPIC_BASE_URL = \"https://www.aigccode.cc\"\n$env:ANTHROPIC_AUTH_TOKEN = \"YOUR_API_KEY\"\n```\n\n**macOS（zsh）**\n\n```bash\nexport ANTHROPIC_BASE_URL=\"https://www.aigccode.cc\"\nexport ANTHROPIC_AUTH_TOKEN=\"YOUR_API_KEY\"\n```\n\n**Linux（bash）**\n\n```bash\nexport ANTHROPIC_BASE_URL=\"https://www.aigccode.cc\"\nexport ANTHROPIC_AUTH_TOKEN=\"YOUR_API_KEY\"\n```\n\n### 永久设置\n\n**Windows（PowerShell）**\n\n```powershell\n[System.Environment]::SetEnvironmentVariable(\"ANTHROPIC_BASE_URL\", \"https://www.aigccode.cc\", [System.EnvironmentVariableTarget]::User)\n[System.Environment]::SetEnvironmentVariable(\"ANTHROPIC_AUTH_TOKEN\", \"YOUR_API_KEY\", [System.EnvironmentVariableTarget]::User)\n```\n\n**macOS（zsh）**\n\n```bash\necho 'export ANTHROPIC_BASE_URL=\"https://www.aigccode.cc\"' >> ~/.zshrc\necho 'export ANTHROPIC_AUTH_TOKEN=\"YOUR_API_KEY\"' >> ~/.zshrc\nsource ~/.zshrc\n```\n\n**Linux（bash）**\n\n```bash\necho 'export ANTHROPIC_BASE_URL=\"https://www.aigccode.cc\"' >> ~/.bashrc\necho 'export ANTHROPIC_AUTH_TOKEN=\"YOUR_API_KEY\"' >> ~/.bashrc\nsource ~/.bashrc\n```\n\n将 `YOUR_API_KEY` 替换为控制台中复制的 API Key。\n\n---\n\n## 第三步：开始使用\n\n```bash\nclaude\n```\n\n启动后，Claude Code 会分析当前目录代码并提供辅助。\n\n### 常用命令\n\n```bash\n# 交互式启动（在项目目录中）\nclaude\n\n# 单次任务（非交互）\nclaude -p \"解释这个项目的结构\"\n\nclaude --help\nclaude --version\n```\n\n---\n\n## 配置文件说明\n\nClaude Code 会读取：\n\n| 文件 | 位置 | 用途 |\n|------|------|------|\n| `CLAUDE.md` | 项目根目录 | 项目上下文与规范 |\n| `settings.json` | `.claude/settings.json` | 项目级设置 |\n| `~/.claude.json` | 用户主目录 | 全局偏好 |\n\n可在项目根目录添加 `CLAUDE.md`，例如：\n\n```markdown\n# 示例 CLAUDE.md\n这是一个 React + TypeScript 项目。\n代码风格：2 空格缩进，函数组件优先。\n测试框架：Vitest。\n```\n\n---\n\n## 常见问题排查\n\n| 现象 | 处理 |\n|------|------|\n| `Authentication error` | 检查 `ANTHROPIC_AUTH_TOKEN` |\n| 启动后无响应 | 检查 `ANTHROPIC_BASE_URL` |\n| `Cannot find module` | 重新执行全局安装命令 |\n| 国内网络超时 | 确认 `ANTHROPIC_BASE_URL` 为 `https://www.aigccode.cc` |\n| macOS 权限不足 | 使用 `sudo npm install -g` 或调整 npm 全局目录 |\n| Windows 环境变量不生效 | 重新打开 PowerShell，或注销后重新登录 |\n\n更多说明见 [Anthropic Claude Code 文档](https://docs.anthropic.com/en/docs/claude-code)。\n\n---\n\n## 相关教程\n\n- 上一篇：[Node.js 环境安装](?doc=nodejs)\n- 下一篇：[Gemini CLI 配置](?doc=gemini)\n"},{"id":"gemini","title":"Gemini CLI 配置","md":"# Gemini CLI 配置教程\n\n> 安装并配置 Gemini CLI，连接 Ai Go Code 平台使用 Gemini 模型。\n\n[Gemini CLI](https://cloud.google.com/gemini/docs/cli) 是 Google 提供的终端 AI 助手。本教程覆盖安装与环境配置。\n\n**前置条件：** 请先完成 [Node.js 环境安装](?doc=nodejs)（需要 **Node.js ≥ 18**）。\n\n---\n\n## 第一步：安装 Gemini CLI\n\n**Windows / Linux（npm）**\n\n```bash\nnpm install -g @google/gemini-cli\n```\n\n**macOS（npm）**\n\n```bash\nnpm install -g @google/gemini-cli\n```\n\n**macOS（Homebrew）**\n\n```bash\nbrew install gemini-cli\n```\n\n### 验证安装\n\n```bash\ngemini --version\n```\n\n---\n\n## 第二步：配置环境变量\n\n需设置：\n\n| 变量 | 说明 |\n|------|------|\n| `GOOGLE_GEMINI_BASE_URL` | 服务地址 |\n| `GEMINI_API_KEY` | API Key |\n| `GEMINI_MODEL` | 使用的模型 |\n\n### 临时设置（当前会话）\n\n**Windows（PowerShell）**\n\n```powershell\n$env:GOOGLE_GEMINI_BASE_URL = \"https://www.aigccode.cc\"\n$env:GEMINI_API_KEY = \"YOUR_API_KEY\"\n$env:GEMINI_MODEL = \"gemini-3-pro-preview\"\n```\n\n**macOS（zsh）**\n\n```bash\nexport GOOGLE_GEMINI_BASE_URL=\"https://www.aigccode.cc\"\nexport GEMINI_API_KEY=\"YOUR_API_KEY\"\nexport GEMINI_MODEL=\"gemini-3-pro-preview\"\n```\n\n**Linux（bash）**\n\n```bash\nexport GOOGLE_GEMINI_BASE_URL=\"https://www.aigccode.cc\"\nexport GEMINI_API_KEY=\"YOUR_API_KEY\"\nexport GEMINI_MODEL=\"gemini-3-pro-preview\"\n```\n\n### 永久设置\n\n**Windows（PowerShell）**\n\n```powershell\n[System.Environment]::SetEnvironmentVariable(\"GOOGLE_GEMINI_BASE_URL\", \"https://www.aigccode.cc\", [System.EnvironmentVariableTarget]::User)\n[System.Environment]::SetEnvironmentVariable(\"GEMINI_API_KEY\", \"YOUR_API_KEY\", [System.EnvironmentVariableTarget]::User)\n[System.Environment]::SetEnvironmentVariable(\"GEMINI_MODEL\", \"gemini-3-pro-preview\", [System.EnvironmentVariableTarget]::User)\n```\n\n**macOS（zsh）**\n\n```bash\necho 'export GOOGLE_GEMINI_BASE_URL=\"https://www.aigccode.cc\"' >> ~/.zshrc\necho 'export GEMINI_API_KEY=\"YOUR_API_KEY\"' >> ~/.zshrc\necho 'export GEMINI_MODEL=\"gemini-3-pro-preview\"' >> ~/.zshrc\nsource ~/.zshrc\n```\n\n**Linux（bash）**\n\n```bash\necho 'export GOOGLE_GEMINI_BASE_URL=\"https://www.aigccode.cc\"' >> ~/.bashrc\necho 'export GEMINI_API_KEY=\"YOUR_API_KEY\"' >> ~/.bashrc\necho 'export GEMINI_MODEL=\"gemini-3-pro-preview\"' >> ~/.bashrc\nsource ~/.bashrc\n```\n\n将 `YOUR_API_KEY` 替换为控制台中的 Key；更换模型时修改 `GEMINI_MODEL` 即可。\n\n### 可选：配置文件 `settings.json`\n\n| 路径 | 作用范围 |\n|------|----------|\n| `~/.gemini/settings.json` | 全局 |\n| `./.gemini/settings.json` | 项目级（优先级更高） |\n\n示例：\n\n```json\n{\n  \"selectedAuthType\": \"gemini-api-key\",\n  \"model\": \"gemini-3-pro-preview\",\n  \"baseUrl\": \"https://www.aigccode.cc\",\n  \"apiKey\": \"YOUR_API_KEY\"\n}\n```\n\n---\n\n## 第三步：开始使用\n\n在项目目录执行：\n\n```bash\ngemini\n```\n\n### 常用命令\n\n```bash\ngemini -p \"你好\"\ngemini \"帮我解释这段代码\"\ngemini \"分析这个项目的结构并给出优化建议\"\n```\n\n### 交互模式内指令\n\n| 指令 | 说明 |\n|------|------|\n| `/help` | 帮助 |\n| `/clear` | 清空对话 |\n| `/quit` 或 `/exit` | 退出 |\n| `/model` | 切换模型 |\n| `/tools` | 可用工具 |\n| `@文件名` | 引用文件 |\n| `!命令` | 执行 Shell |\n\n---\n\n## 常见问题排查\n\n| 现象 | 处理 |\n|------|------|\n| `API key not valid` | 检查 `GEMINI_API_KEY` |\n| 连接超时 | 确认 `GOOGLE_GEMINI_BASE_URL` 为 `https://www.aigccode.cc` |\n| `Model not found` | 检查 `GEMINI_MODEL` 名称 |\n| `Rate limit exceeded` | 降低请求频率后重试 |\n| Windows 环境变量不生效 | 重新打开 PowerShell |\n\n更多说明见 [Google Gemini CLI 文档](https://cloud.google.com/gemini/docs/cli)。\n\n---\n\n## 相关教程\n\n- 上一篇：[Claude Code 配置](?doc=claude)\n- 下一篇：[Codex 配置](?doc=codex)\n"},{"id":"codex","title":"Codex 配置","md":"# Codex（OpenAI）配置教程\n\n> 安装并配置 Codex CLI，通过配置文件连接 Ai Go Code 平台。\n\nCodex 是支持 **OpenAI 兼容接口** 的 CLI，主要通过配置文件完成连接。本教程覆盖安装与配置。\n\n**前置条件：** 请先完成 [Node.js 环境安装](?doc=nodejs)（Codex CLI 推荐 **Node.js ≥ 22**）。\n\n---\n\n## 第一步：安装 Codex\n\n```bash\nnpm install -g @openai/codex@latest\n```\n\n### 验证安装\n\n```bash\ncodex --version\n```\n\n---\n\n## 第二步：创建配置文件\n\n需要 `config.toml` 与 `auth.json`，位于用户目录下的 `.codex` 文件夹。\n\n### 创建配置目录\n\n**Windows（PowerShell）**\n\n```powershell\nif (Test-Path \"$env:USERPROFILE\\.codex\") { Remove-Item -Recurse -Force \"$env:USERPROFILE\\.codex\" }\nmkdir \"$env:USERPROFILE\\.codex\"\n```\n\n**macOS / Linux**\n\n```bash\nmkdir -p ~/.codex\n```\n\n### 写入配置文件\n\n**Windows（PowerShell）**\n\n```powershell\n# config.toml\n@\"\nmodel_provider = \"aigccode\"\nmodel = \"gpt-5.3-codex\"\nmodel_reasoning_effort = \"high\"\ndisable_response_storage = true\npreferred_auth_method = \"apikey\"\n\n[model_providers.aigccode]\nname = \"aigccode\"\nbase_url = \"https://www.aigccode.cc/v1\"\nwire_api = \"responses\"\nrequires_openai_auth = true\n\"@ | Out-File -FilePath \"$env:USERPROFILE\\.codex\\config.toml\" -Encoding utf8\n\n# auth.json\n@\"\n{\n  \"OPENAI_API_KEY\": \"YOUR_API_KEY\"\n}\n\"@ | Out-File -FilePath \"$env:USERPROFILE\\.codex\\auth.json\" -Encoding utf8\n```\n\n**macOS / Linux**\n\n```bash\ncat > ~/.codex/config.toml << 'EOF'\nmodel_provider = \"aigccode\"\nmodel = \"gpt-5.3-codex\"\nmodel_reasoning_effort = \"high\"\ndisable_response_storage = true\npreferred_auth_method = \"apikey\"\n\n[model_providers.aigccode]\nname = \"aigccode\"\nbase_url = \"https://www.aigccode.cc/v1\"\nwire_api = \"responses\"\nrequires_openai_auth = true\nEOF\n\ncat > ~/.codex/auth.json << 'EOF'\n{\n  \"OPENAI_API_KEY\": \"YOUR_API_KEY\"\n}\nEOF\n\nchmod 600 ~/.codex/auth.json\n```\n\n将 `YOUR_API_KEY` 替换为控制台中的 API Key。\n\n### 配置项说明\n\n| 配置项 | 说明 |\n|--------|------|\n| `model` | 模型名，如 `gpt-5.3-codex`、`o4-mini`、`o3` |\n| `model_reasoning_effort` | 推理强度：`low` / `medium` / `high` |\n| `disable_response_storage` | 是否禁用服务端响应存储 |\n| `preferred_auth_method` | API Key 时填 `apikey` |\n\n---\n\n## 第三步：开始使用\n\n```bash\ncodex\n```\n\n### 常用命令\n\n```bash\ncodex\ncodex \"解释这个函数做了什么\"\ncodex --model o4-mini \"帮我写单元测试\"\n\n# 审批模式：suggest（默认）/ auto-edit / full-auto\ncodex --approval-mode auto-edit \"重构这个模块\"\n\ncodex --cwd /path/to/project \"分析项目结构\"\ncodex --quiet \"列出所有 TODO 注释\"\n```\n\n---\n\n## 常见问题排查\n\n| 现象 | 处理 |\n|------|------|\n| `Authentication failed` | 检查 `auth.json` 中的 Key |\n| `Model not found` | 检查 `config.toml` 中的 `model` |\n| `Connection refused` | 确认 `base_url` 为 `https://www.aigccode.cc/v1` |\n| 配置不生效 | 确认路径：`~/.codex/config.toml` 与 `~/.codex/auth.json` |\n| Windows 路径问题 | 确认文件在 `%USERPROFILE%\\.codex\\` |\n\n更多说明见 [OpenAI Codex 文档](https://platform.openai.com/docs/codex)。\n\n---\n\n## 相关教程\n\n- 上一篇：[Gemini CLI 配置](?doc=gemini)\n"}];
